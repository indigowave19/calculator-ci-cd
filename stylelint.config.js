module.exports = {
  extends: [
    "stylelint-config-standard" // Extends standard rules
  ],
  rules: {
    "indentation": [2],           // Enforce 2-space indentation
    "string-quotes": ["double"],  // Enforce double quotes for strings
  },
};
